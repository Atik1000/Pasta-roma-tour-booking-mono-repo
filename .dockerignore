node_modules
**/node_modules
.next
**/.next
dist
**/dist
.turbo
**/.turbo
coverage
**/coverage
.git
.github
.env
.env.*
!.env.example
*.log
apps/api/src/generated
docker/*.yml
docs
*.md
!README.md

# Design references (Figma exports, ~27 MB). No build reads them — the only
# thing they did was travel to the Docker daemon on every single build.
# These match at the context root only, so app source of the same name is safe.
Admin Panel
Blogs
Cart
Cart checkout_ Booking Checkout
Check Ticket availability
Landing page
My Bookings
Tours
